# https://github.com/P3TERX/Actions-OpenWrt
#
# File: .github/workflows/openwrt-builder.yml
# Description: Build OpenWrt using GitHub Actions
#
# Copyright (c) 2019-2024 P3TERX <https://p3terx.com>
#
# This is free software, licensed under the MIT License.
# See /LICENSE for more information.
#

name: OpenWrt Builder (RAX3000M)

on:
  repository_dispatch:
  workflow_dispatch:
    inputs:
      enable_5g_25db:
        description: '启用 5G 25dB 修改'
        required: false
        type: boolean
        default: true

permissions:
  contents: write

env:
  REPO_URL: https://github.com/padavanonly/immortalwrt-mt798x-6.6
  REPO_BRANCH: openwrt-24.10-6.6
  DEVICE_NAME: rax3000m
  ENABLE_5G_25DB: ${{ github.event.inputs.enable_5g_25db || 'true' }}
  FEEDS_CONF: feeds.conf.default
  CONFIG_FILE: .config
  DIY_P1_SH: diy-part1.sh
  DIY_P2_SH: diy-part2.sh
  UPLOAD_BIN_DIR: false
  UPLOAD_FIRMWARE: true
  UPLOAD_RELEASE: true
  TZ: Asia/Shanghai

jobs:
  build:
    runs-on: ubuntu-22.04
    steps:
      - name: 检出代码 (Checkout)
        uses: actions/checkout@v4

      - name: 初始化编译环境 (Initialization environment)
        env:
          DEBIAN_FRONTEND: noninteractive
        run: |
          sudo rm -rf /etc/apt/sources.list.d/* /usr/share/dotnet /usr/local/lib/android /opt/ghc /opt/hostedtoolcache/CodeQL
          sudo docker image prune --all --force
          sudo -E apt-get -qq update
          sudo -E apt-get -qq install ack antlr3 asciidoc autoconf automake autopoint binutils bison build-essential bzip2 ccache cmake cpio curl device-tree-compiler fastjar flex gawk gettext gcc-multilib g++-multilib git gperf haveged help2man intltool libc6-dev-i386 libelf-dev libfuse-dev libglib2.0-dev libgmp3-dev libltdl-dev libmpc-dev libmpfr-dev libncurses5-dev libncursesw5-dev libpython3-dev libreadline-dev libssl-dev libtool lrzsz mkisofs msmtp ninja-build p7zip p7zip-full patch pkgconf python2.7 python3 python3-pyelftools python3-setuptools qemu-utils rsync scons squashfs-tools subversion swig texinfo uglifyjs upx-ucl unzip vim wget xmlto xxd zlib1g-dev
          sudo -E apt-get -qq autoremove --purge
          sudo -E apt-get -qq clean
          sudo timedatectl set-timezone "$TZ"
          sudo mkdir -p /workdir
          sudo chown $USER:$GROUPS /workdir

      - name: 克隆源码 (Clone source code)
        working-directory: /workdir
        run: |
          df -hT $PWD
          git clone $REPO_URL -b $REPO_BRANCH openwrt
          ln -sf /workdir/openwrt $GITHUB_WORKSPACE/openwrt

      - name: 加载自定义 Feeds (Load custom feeds)
        run: |
          [ -e $FEEDS_CONF ] && mv $FEEDS_CONF openwrt/feeds.conf.default
          chmod +x $DIY_P1_SH
          cd openwrt
          $GITHUB_WORKSPACE/$DIY_P1_SH

      - name: 更新 Feeds (Update feeds)
        run: cd openwrt && ./scripts/feeds update -a

      - name: 安装 Feeds (Install feeds)
        run: cd openwrt && ./scripts/feeds install -a

      - name: 加载自定义配置 (Load custom configuration)
        run: |
          [ -e files ] && mv files openwrt/files
          [ -e $CONFIG_FILE ] && mv $CONFIG_FILE openwrt/.config
          chmod +x $DIY_P2_SH
          cd openwrt
          $GITHUB_WORKSPACE/$DIY_P2_SH

      - name: 修改 5G 25dB
        if: env.ENABLE_5G_25DB == 'true'
        run: |
          cd openwrt
          EEPROM_FILE=package/mtk/drivers/mt_wifi/files/mt7981-default-eeprom/MT7981_iPAiLNA_EEPROM.bin
          
          if [ ! -f "$EEPROM_FILE" ]; then
            echo "⚠️ 警告：找不到 EEPROM 文件：$EEPROM_FILE"
            echo "跳过 5G 25dB 修改。"
            exit 0
          fi

          EXPECTED_CONTENT=$(printf '\x2B\x2B\x2B\x2B\x2B\x2B\x2B\x2B\x2B\x2B\x2B\x2B\x2B\x2B\x2B\x2B\x2B\x2B\x2B\x2B')
          # 读取当前值 (从 0x445 开始读取 20 字节)
          CURRENT_CONTENT=$(dd if="$EEPROM_FILE" bs=1 skip=$((0x445)) count=20 2>/dev/null)
          
          if [ "$CURRENT_CONTENT" != "$EXPECTED_CONTENT" ]; then
            echo "✅ 正在应用 5G 25dB 修改..."
            printf '\x2B\x2B\x2B\x2B\x2B\x2B\x2B\x2B\x2B\x2B\x2B\x2B\x2B\x2B\x2B\x2B\x2B\x2B\x2B\x2B' | dd of="$EEPROM_FILE" bs=1 seek=$((0x445)) conv=notrunc
          else
            echo "ℹ️ 5G 25dB 已经修改过，无需重复操作"
          fi

      # 🔥🔥🔥【刷新】修改 Key 为 reset-cache-v3 🔥🔥🔥
      - name: 缓存加速 (Cache)
        uses: actions/cache@v4
        with:
          path: |
            openwrt/dl
            openwrt/.ccache
          key: ${{ runner.os }}-openwrt-${{ env.REPO_BRANCH }}-reset-cache-v3-${{ hashFiles('openwrt/.config') }}
          restore-keys: |
            ${{ runner.os }}-openwrt-${{ env.REPO_BRANCH }}-reset-cache-v3-

      - name: 下载软件包 (Download package)
        id: package
        run: |
          cd openwrt
          make defconfig
          make download -j8
          find dl -size -1024c -exec ls -l {} \;
          find dl -size -1024c -exec rm -f {} \;

      - name: 编译固件 (Compile the firmware)
        id: compile
        run: |
          cd openwrt
          
          # 启用 CCACHE 加速编译
          echo "CONFIG_CCACHE=y" >> .config
          export CCACHE_DIR=$(pwd)/.ccache
          
          echo -e "$(nproc) thread compile"
          make -j$(nproc) || make -j1 || make -j1 V=s
          echo "status=success" >> $GITHUB_OUTPUT
          
          # 提取版本号
          VERSION=$(git describe --tags --abbrev=0 2>/dev/null || echo "commit-$(git rev-parse --short HEAD)")
          echo "FIRMWARE_VERSION=$VERSION" >> $GITHUB_ENV

      - name: 生成 README 说明
        run: |
          cat << EOF > README.md
          # OpenWrt RAX3000M 固件
          
          - **版本**: ${{ env.FIRMWARE_VERSION }}
          - **设备**: RAX3000M
          - **5G 25dB**: ${{ env.ENABLE_5G_25DB }}
          - **构建时间**: $(date +"%Y-%m-%d %H:%M %Z")
          - **默认地址**: 192.168.6.1 (密码为空)
          
          ## 安装说明
          1. 备份配置
          2. 通过 Web 界面上传固件
          3. 重启设备
          
          ## 注意
          第三方固件，自行风险。支持: https://github.com/padavanonly/immortalwrt-mt798x-6.6
          EOF

      - name: 生成发布描述
        run: |
          cat << EOF > release.txt
          # OpenWrt RAX3000M 固件发布
          
          - **版本**: ${{ env.FIRMWARE_VERSION }}
          - **设备**: RAX3000M
          - **5G 25dB**: ${{ env.ENABLE_5G_25DB }}
          - **时间**: $(date +"%Y-%m-%d %H:%M %Z")
          - **默认地址**: 192.168.6.1 (密码为空)
          
          ## 安装
          1. 备份配置
          2. 通过 Web 界面上传固件
          3. 重启设备
          
          ## 特性
          - ✅ 优化网络性能，支持 USB 共享、HNAT 加速、Ksmbd 共享
          EOF
          
          if [ "${{ env.ENABLE_5G_25DB }}" == "true" ]; then
            echo "- ✅ 5G 25dB 高功率模式" >> release.txt
          fi

      - name: 整理固件文件
        id: organize
        run: |
          cd openwrt/bin/targets/*/*
          rm -rf packages
          echo "FIRMWARE=$PWD" >> $GITHUB_ENV
          echo "status=success" >> $GITHUB_OUTPUT

      - name: 上传固件目录
        uses: actions/upload-artifact@v4
        if: steps.organize.outputs.status == 'success'
        with:
          name: OpenWrt_firmware_${{ env.FIRMWARE_VERSION }}_${{ env.DEVICE_NAME }}
          path: ${{ env.FIRMWARE }}

      - name: 发布固件到 Release
        uses: softprops/action-gh-release@master
        if: steps.organize.outputs.status == 'success'
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        with:
          tag_name: ${{ env.FIRMWARE_VERSION }}-${{ github.sha }}
          body_path: release.txt
          files: ${{ env.FIRMWARE }}/*

      - name: 删除旧的 Release 发布
        uses: dev-drprasad/delete-older-releases@master
        if: steps.organize.outputs.status == 'success'
        with:
          keep_latest: 3
          delete_tags: true
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - name: 删除旧的 Workflow 运行记录
        uses: Mattraks/delete-workflow-runs@v2
        with:
          retain_days: 0
          keep-minimum-runs: 2
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
